[pytest]
# Add the source directory to the Python path
pythonpath = src

# Optional: Specify additional pytest options
# Add any other configurations you might need
# Example:
# addopts = -v  # Verbose output
# testpaths = tests  # Specify the directory for test discovery